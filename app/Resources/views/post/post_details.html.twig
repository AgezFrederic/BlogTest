<article>
    <h2>{{post.title}}</h2>
    <div class="desc">
        {{post.summary}}
    </div>
    <a href="{{path('post_show', {'slug': post.slug})}}">{% trans %}action.see_more{% endtrans %}</a>
</article>